# 什么是 RAG

RAG(Retrieval-Augmented Generation,检索增强生成)是一种把"外部知识检索"和"大语言模型生成"结合起来的技术方案:模型先从知识库中检索出与问题相关的资料片段,再把资料和问题一起交给大模型,让回答有据可依。

RAG 解决两个核心问题:一是大模型的知识停留在训练数据截止时间,无法感知新知识;二是模型可能"一本正经地编造"(幻觉),而引用真实资料能显著降低幻觉。

RAG 有两个阶段:离线建库(ingest)把文档切块并向量化存储;在线问答(query)把问题向量化、检索最相关片段、组装提示词、生成带引用的回答。
