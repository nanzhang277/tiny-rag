# RAG 流水线的环节

一条完整的 RAG 流水线包含:文档加载、切块(Chunking)、向量化(Embedding)、检索(Retrieval)、重排(Rerank)、生成(Generation)、评测(Evaluation)。

切块把长文档切成适合检索的小段,常见策略有固定长度切分与递归切分。向量化把文本映射成高维向量,语义相近的文本向量夹角更小。检索常用余弦相似度取 top-k;混合检索再叠加 BM25 关键词信号并用 RRF 融合。重排用更重的模型对候选精排,体现"检索不等于排序"。评测用 recall@k、MRR 等指标量化检索质量,用 LLM 自评考察忠实度与相关性。
